<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AppCompat.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/pomadeRed</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splashdd</item>

        <item name="postSplashScreenTheme">@style/Theme.DateDating</item>
        <item name="android:navigationBarColor">
            @android:color/black
        </item>
        <item name="android:statusBarColor">
            @android:color/transparent
        </item>
        <item name="android:windowLightStatusBar">
            true
        </item>

    </style>
</resources>